apiVersion: v1
kind: Service
metadata:
  name: angular-ssr
  namespace: angular-ssr
  labels:
    app: angular-ssr
spec:
  type: ClusterIP
  selector:
    app: angular-ssr
  ports:
  - port: 80
    targetPort: 4200
    protocol: TCP
    name: http
